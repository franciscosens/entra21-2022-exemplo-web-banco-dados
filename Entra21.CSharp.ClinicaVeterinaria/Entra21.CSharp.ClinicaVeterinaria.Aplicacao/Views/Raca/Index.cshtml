@model List<Raca>

<p class="h3">Lista de Raças</p>

<div class="row mb-3">
    <div class="text-end">
        <a asp-action="cadastrar" class="btn btn-primary">
            <i class="fa-solid fa-plus"></i> Cadastrar
        </a>
    </div>
</div>

<table class="table table-striped table-bordered">
    <thead>
    <!-- Cabeçalho da tabela-->
    <tr>
        <!-- Linha da tabela-->
        <th>Nome</th> <!-- Coluna específica do cabeçalho-->
        <th>Espécie</th>
        <th>Ação</th>
    </tr>
    </thead>
    <tbody>
    <!-- Lugar dos registros da tabela-->
    @foreach (var raca in Model)
    {
        <tr>
            <!-- Linha-->
            <td>@raca.Nome</td> <!-- Coluna específica para body-->
            <td>@raca.Especie</td>
            <td>
                <a class="btn btn-primary" asp-action="editar" asp-route-id="@raca.Id">
                    <i class="fa-solid fa-pencil"></i> Editar
                </a>
                <a class="btn btn-outline-danger" asp-action="apagar" asp-route-id="@raca.Id">
                    <i class="fa-solid fa-trash"></i> Apagar
                </a>
            </td>
        </tr>
    }
    </tbody>
</table>